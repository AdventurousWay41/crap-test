name: Hello World
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Hello World
      run: echo "Hello, World!"

    - name: Checkout code
      uses: actions/checkout@v4
      

    - name: List files
      run: ls -la
        